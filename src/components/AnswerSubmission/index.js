import React from 'react';
import './index.css';

const AnswerSubmission = (props) => {
    const {
    } = props;

    return (
        <h2>AnswerSubmission</h2>
    );
};

export default AnswerSubmission;
